# Copy this file to `.env.local` (same folder) and paste the Resend API key.
# `.env.local` is gitignored — never commit it.

RESEND_API_KEY=PASTE_RESEND_KEY_HERE

# Already verified — leave these alone.
RESEND_FROM_DOMAIN=contact.coldsignal.dev
RESEND_FROM_NAME=Coldsignal
RESEND_REPLY_TO=hello@contact.coldsignal.dev
